{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "gobarber",
  "entities": [
    "@shared/infra/typeorm/migrations/*.ts"
  ],
  "migrations": [
    "@shared/infra/typeorm/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "@shared/infra/typeorm/migrations"
  }
}
